[README.md](https://github.com/user-attachments/files/26864630/README.md)
# 박스권 스캐너 컨트롤룸

한국 주식시장(KOSPI / KOSDAQ)에서 박스권 종목을 자동 탐지하는 스캐너.

🔗 **배포 주소**: https://appapppy-arrvupjezplekjddatmdmg.streamlit.app

---

## 📁 파일 구조

```
box-range-scanner/
├─ streamlit_app.py       ← UI 메인 (v9.0)
├─ box_range_scanner.py   ← 스캔 로직 모듈 (v9.0)
├─ requirements.txt       ← 설치 목록
└─ README.md              ← 이 파일
```

---

## 🏪 시장 선택

사이드바에서 스캔할 시장을 선택할 수 있습니다.

| 옵션 | 설명 |
|------|------|
| KOSPI | 코스피 전종목 |
| KOSDAQ | 코스닥 전종목 |
| ALL | KOSPI + KOSDAQ 전체 병합 (종목코드 순 정렬) |

---

## 🔍 스캔 모드

### ⚡ 빠른 스캔 (후보군)
- 거래량 상위 종목 기준 (사이드바에서 수량 설정)
- 시장별 거래량 상위 종목 자동 수집
- 빠르게 결과 확인 가능

### 🔍 전체 스캔 (전종목)
- 선택한 시장 전종목 대상
- threshold 슬라이더로 점수 기준 조정 가능
- 진행바로 실시간 진행 확인
- 소요 시간: 수 분

---

## 🔌 데이터 소스 구조 (멀티소스 fallback)

스캐너는 어떤 상황에서도 멈추지 않도록 데이터 소스를 순차적으로 시도합니다.

### ticker 확보 순서
```
1. FDR StockListing (KOSPI / KOSDAQ)
2. 최근 성공 캐시
3. fallback (시장별 고정 종목)
```

### 가격 데이터 확보 순서
```
1. FDR-KRX
2. FDR-NAVER
3. yfinance
4. 최근 성공 캐시
5. pykrx (최종 fallback)
```

> 한 스캔 내에서 소스는 단일 고정 — 종목별로 소스를 섞지 않습니다.

---

## 🖥️ 컨트롤룸 UI

결과 상단에 현재 데이터 수집 상태를 한 줄로 표시합니다.

| 배지 | 내용 |
|------|------|
| 시장 | 선택한 시장 (KOSPI / KOSDAQ / ALL) |
| ticker 소스 | FDR / CACHE / FALLBACK |
| 가격 소스 | FDR-KRX / FDR-NAVER / YFINANCE / CACHE / PYKRX |
| 모드 | 전체 스캔 / 빠른 스캔 |
| 캐시 | 캐시 사용 시 날짜 표시 |

### 상태별 컬러
| 상태 | 표시 | 의미 |
|------|------|------|
| ✅ 정상 | 초록 | FDR 기반 실시간 데이터 |
| ⚠️ 경고 | 노랑 | 캐시 데이터 사용 중 (날짜 포함) |
| 🚨 위기 | 빨강 | 제한 모드 (fallback 종목 기준) |

---

## 📊 결과 화면

| 컬럼 | 설명 |
|------|------|
| 종목코드 | KRX 종목코드 |
| 종목명 | 종목 이름 |
| 점수 | 박스권 점수 (0~100, 높을수록 박스권) |
| 거래량 | 최근 거래량 |
| 이유 | 감점 사유 또는 "박스권 안정" |
| 돌파신호 | 🟢 상단돌파임박 / 🟡 박스권상단 / ⚪ 박스권중립 / 🔴 하단이탈임박 |

결과 요약 예시:
```
KOSPI 950개 중 42개 박스권 후보 (4.4%) | threshold 70점
```

---

## 🔖 점수 기준 (연속 점수 체계)

감점은 각 조건을 선형 보간으로 계산합니다 (최대 각 33.3점).

| 조건 | 이상 기준 | 위험 기준 |
|------|-----------|-----------|
| 변동폭 (고가-저가 / 평균가) | ≤ 10% | ≥ 40% |
| 변동성 (일일 수익률 표준편차) | ≤ 1% | ≥ 4% |
| MA 기울기 (상대값) | ≤ 0.3% | ≥ 1.5% |

| 점수 | 의미 |
|------|------|
| 100점 | 박스권 가능성 매우 높음 |
| 70점 내외 | 보통 |
| 40점 이하 | 추세 진행 중 |

---

## ⚠️ 주의사항

| 항목 | 내용 |
|------|------|
| KRX 마스터 API | Streamlit Cloud에서 `get_market_ticker_list()` 사용 불가 → FDR StockListing 기반으로 전환됨 |
| 제한 모드 편향 | fallback 종목은 시장별 대형주 편향 → threshold 튜닝 수치는 전체 스캔 기준으로 확정할 것. 제한 모드 수치는 참고용만 |
| 주말/공휴일 | 데이터 수집 실패 시 fallback으로 자동 전환 (정상 동작) |
| 코드 복붙 주의 | 코드 복사 시 따옴표가 곱따옴표로 깨질 수 있음 → GitHub에서 직접 파일 다운로드해서 사용할 것 |
| ALL 스캔 소요 시간 | KOSPI + KOSDAQ 전종목 병합 시 스캔 시간이 두 배로 증가 |

---

## ⚙️ 로컬 실행 방법

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

---

## ❗ 오류 대응

| 증상 | 원인 | 해결 |
|------|------|------|
| 경고 배지 표시 | 캐시 데이터 사용 중 | 평일 장 마감 후 재실행 |
| 위기(빨강) 배지 표시 | 모든 소스 실패 → fallback 모드 | 잠시 후 재시도 |
| `ModuleNotFoundError` | 패키지 미설치 | `pip install -r requirements.txt` 재실행 |
| 결과 없음 | threshold가 너무 높음 | 사이드바 슬라이더로 threshold 낮추기 |

---

## 📌 버전 히스토리

| 버전 | 주요 변경 |
|------|-----------|
| v9.0 | 입력 레이어 리팩토링 — 멀티소스 fallback (FDR→NAVER→yfinance→캐시), 시장 선택 UI (KOSPI/KOSDAQ/ALL), 컨트롤룸 배지, 상태별 컬러 |
| v8.3 | threshold 슬라이더, 튜닝 권고 배너 |
| v8.2 | 점수 체계 연속화 (0~100) |
| v8.1 | get_market_ohlcv_by_ticker 기반 전환, processed_count 분리 |
| v8.0 | 전체 스캔 모드 추가 |
| v7.2 | 거래량 기준 후보 생성 |
| v7.0 | 시총 상위 100개 자동 후보 생성 |
| v6.0 | 돌파신호 추가 |
